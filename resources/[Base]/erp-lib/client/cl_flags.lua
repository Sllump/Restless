function SetPedFlag(object, flag, enabled)
    exports['erp-flags']:SetPedFlag(object, flag, enabled)
end

function HasPedFlag(object, flag)
    return exports['erp-flags']:HasPedFlag(object, flag)
end

function GetPedFlags(object)
    return exports['erp-flags']:GetPedFlags(object)
end

function SetPedFlags(object, flag)
    exports['erp-flags']:SetPedFlags(object, flag)
end

function SetVehicleFlag(vehicle, flag, enabled)
    exports['erp-flags']:SetVehicleFlag(vehicle, flag, enabled)
end

function HasVehicleFlag(vehicle, flag)
    return exports['erp-flags']:HasVehicleFlag(vehicle, flag)
end

function GetVehicleFlags(vehicle)
    return exports['erp-flags']:GetVehicleFlags(vehicle)
end

function SetVehicleFlags(object, flag)
    exports['erp-flags']:SetVehicleFlags(object, flag)
end

function SetObjectFlag(object, flag, enabled)
    exports['erp-flags']:SetObjectFlag(object, flag, enabled)
end

function HasObjectFlag(object, flag)
    return exports['erp-flags']:HasObjectFlag(object, flag)
end

function GetObjectFlags(object)
    return exports['erp-flags']:GetObjectFlags(object)
end

function SetObjectFlags(object, flag)
    exports['erp-flags']:SetObjectFlags(object, flag)
end